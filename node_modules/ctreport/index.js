// import CTReport from './HelloWorld.vue'
var CTReport = require('./HelloWorld.vue');

const ctReport={
  install:function (Vue) {
    Vue.component('vCTreport',CTReport)
  }
};
// 这里的判断很重要
if (typeof window !== 'undefined' && window.Vue) {
  window.Vue.use(ctReport)
}
console.log(CTReport)
// export default CTReport
module.exports = CTReport
