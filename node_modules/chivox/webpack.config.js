const path = require('path');
module.exports = {
    entry:'./index.js',
    output:{
        path:path.resolve(__dirname,'dist'),
        filename:'chivox.min.js'
    },
    module:{
        rules:[
            {
                test:/\.js/,use:'babel-loader'
            },
            {
                test: /\.(wav|mp3)$/,
                use: 'file-loader?name=[path][name]-[hash].[ext]'
            }
        ]
    },
    mode:'production'
};
