import { AudioCore } from '../../core/AudioCore';
export declare class AudioModelMobile extends AudioCore {
    constructor(options: any);
    initAudioMobile(): void;
    private progressTouchStart;
    progressTouchMove(e: any): void;
    progressTouchEnd(): void;
}
