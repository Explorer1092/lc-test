export declare class Widgets {
    query(dom: string): Element;
    uuid(): string;
    formatTime(time: number): string;
    merge(obj1: object, obj2: object): any;
    getPosition(node: any): {
        left: any;
        top: any;
    };
}
